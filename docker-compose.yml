services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: business-signal-postgres
    environment:
      POSTGRES_DB: business_signal
      POSTGRES_USER: business_signal
      POSTGRES_PASSWORD: business_signal
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
